include:
  - project: cta-computing/common/aiv-toolkit
    ref: 73f6fb0f6e3df76a825ab082d77f656fe8de6c96
    file: ci-functions.yml
  - aiv-config.yml

stages:
  - prepare
  - lint
  - build
  - sign
  - tests
  - sonarqube
  - publish
  - report
  - changelog

build-docs:
  rules:
    - when: never


pages:
  stage: deploy
  before_script:
  - pip install -r requirements-docs.txt
  script:
  - mkdocs build --strict --site-dir public
  artifacts:
    paths: [public]
include:
- project: cta-computing/common/aiv-toolkit
  ref: 9e83105d7c2b077c6ec944f4de7408650440bbc6
  file: ci-functions.yml
